import: intrinsics i | * | ;
import: core::bool b | if | ;
import: core::cmp c | lt | ;

export: Vec sum-to ;

type: Vec items [i64 8] used usize ;

: sum-to ( i64 -- i64 )
  | n |
  n 0 lt ~[ 0 ] ~[ n ] if ;
