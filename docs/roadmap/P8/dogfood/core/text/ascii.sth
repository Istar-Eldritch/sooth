\ core::text::ascii -- name derives from the path, not from a manifest entry.
import: intrinsics i | * | ;
import: cmp c | lt gt | ;
import: bool b | if | ;

export: upper? lower? ;

: upper? ( u8 -- bool ) | c | c 65 gte c 90 lte and ;
: lower? ( u8 -- bool ) | c | c 97 gte c 122 lte and ;
