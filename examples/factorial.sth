import: intrinsics * ;
import: hosted::show | . | ;
import: core::prelude * ;

: factorial ( i64 -- i64 )
  dup 0 eq ~[
    drop 1
  ] ~[
    dup 1 sub factorial mul
  ] if ;

: main ( -- )
  5 factorial . ;    \ prints 120
