import: intrinsics * ;
import: hosted::show | . | ;
import: core::prelude * ;    \ Bool comes through here, the prelude's own re-export.

: pos ( i64 -- Bool )
  0 gt ;

: classify ( i64 -- i64 )
  pos ~[ 1 ] ~[ 0 ] if ;

: main ( -- )
  7 classify . ;    \ prints 1
